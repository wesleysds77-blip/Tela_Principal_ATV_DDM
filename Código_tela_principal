import { SafeAreaView, Image, StyleSheet, ScrollView, Text, View, Pressable } from 'react-native';
import { StatusBar } from "expo-status-bar";

export default function App() {
  return (
    <SafeAreaView style={styles.container}>
      
      <View style={styles.texto}>

        <Text style={styles.textoConteudo}>
          Aplicativo criado para a visualização de imagens do templo maia.
        </Text>

      </View>

      <ScrollView style={styles.scrollViewStyle} contentContainerStyle={styles.scrollContent}> 

        <Pressable
          onPress={() => { alert("Essas imagens utilizam efeitos especiais para melhorar a visualização do usuário.") }}
        > 
          <Image
            source={{ uri: "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQ1gOYhj_HmxIQj42Xkn0acnB5mGVaGU9zZzOa7EgkZTg&s=10" }}
            style={styles.imagem}
          />

          <Image
            source={{ uri: "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRHcuWWAg1X9wPRvhr_c7LNhKWq-mORU12MSoOnKHZk-Q&s=10" }}
            style={styles.imagem}
          />

          <Image
            source={{ uri: "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcT3cYGFTwEA9UwrVxY5K-AcysVIc3HbIpErf8pC4F8WZQ&s=10" }}
            style={styles.imagem}
          />
        </Pressable>

      </ScrollView>

      <View style={styles.rodapeContainer}>

        <Text style={styles.rodape}>
          2026 Wesley Santos da Silva. Todos os direitos reservados
        </Text>

      </View>
        
      <StatusBar style='light' />
      
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000000',
    paddingTop: 30,
  },
  texto: {
    marginHorizontal: 20,
    marginTop: 20,
    marginBottom: 10,
    padding: 16,
    backgroundColor: '#000000',
    borderWidth: 1,
    borderColor: '#FFFFFF',
    borderRadius: 8,
  },
  textoConteudo: {
    color: '#FFFFFF',
    fontSize: 16,
    textAlign: 'center',
    fontFamily: 'serif',
    lineHeight: 22,
  },
  scrollViewStyle: {
    flex: 1,
    backgroundColor: '#000000',
    width: '100%',
  },
  scrollContent: {
    alignItems: 'center',
    paddingVertical: 10,
  },
  imagem: {
    width: 300,
    height: 280,
    borderWidth: 2,
    borderColor: '#FFFFFF',
    marginVertical: 12,
    borderRadius: 8,
  },
  rodapeContainer: {
    width: '100%',
    alignItems: 'center',
    backgroundColor: '#000000',
  },
  rodape: {
    margin: 15,
    paddingVertical: 8,
    paddingHorizontal: 25,
    fontSize: 14,
    fontWeight: 'bold',
    textAlign: 'center',
    backgroundColor: '#000000',
    borderWidth: 1,
    borderColor: '#FFFFFF',
    color: '#FFFFFF',
    letterSpacing: 2,
    borderRadius: 4,
  },
});
